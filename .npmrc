# Cloudflare Workers Builds runs `npm clean-install`; Better Auth 1.6.23 wants kysely ^0.28.5
# while kysely-d1 pins 0.27 — the same conflict `npm install --legacy-peer-deps` sidesteps locally.
legacy-peer-deps=true
